.pagination {
    text-align: center;
    background: white;
    position: relative;
    height: 22px;

    a, .current {
        display: inline-block;
        padding: 0 10px;
        color: $black-2;
        background: white;
    }

    .current {
        color: $gray-2;
    }

    .prev, .next {
        position: absolute;
        vertical-align: middle;
    }

    .prev {
        text-align: left;
        left: 0;

        &::before {
            fa('\f053');
            vertical-align: middle;
            margin-right: 10px;
        }
    }

    .next {
        right: 0;
        text-align: right;

        &::after {
            fa('\f054');
            vertical-align: middle;
            margin-left: 10px;
        }
    }

    .next-text, .prev-text {
        max-width: 240px;
        text-overflow: ellipsis;
        overflow: hidden;
        white-space: nowrap;
        display: inline-block;
        vertical-align: middle;
    }
}